package com.taetae.interfaces.demo.implementation;

import com.taetae.interfaces.demo.Validator;

public class ValidAge implements Validator {
	
	public boolean isvalid(String number ) {
		
		
		 
	            int age = int.parseInt(number); 
	            
	            // String to int conversion 
	            if {
	            return age > 0;
	        }  
	            else {
	            return false;


}
}
}	
	
